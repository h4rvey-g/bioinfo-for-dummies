% Beautybook PDF setup for Quarto
\usepackage{fontspec}

% Fonts are provided by ctex (fontset=fandol) for portability.

% Use Arabic numerals for chapter numbers (affects cross-references)
\ctexset{
  chapter = { number = \arabic{chapter} }
}

% Full-page PDF cover on the first page
\usepackage{graphicx}
\usepackage{eso-pic}
\usepackage{twemojis}
\usepackage{fvextra}
\usepackage{suffix}

% Auto-wrap long code lines (Pandoc Highlighting + verbatim)
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{%
  breaklines,
  breakanywhere,
  breaknonspaceingroup,
  commandchars=\\\{\}%
}

% Version info (auto-filled at render time)
\providecommand{\bookversion}{unknown}
\providecommand{\bookversiondate}{unknown}
\InputIfFileExists{include/version.tex}{}{}
\RecustomVerbatimEnvironment{verbatim}{Verbatim}{%
  breaklines,
  breakanywhere,
  breaknonspaceingroup%
}

% Breakable shaded code blocks across pages
\definecolor{shadecolor}{RGB}{248,248,248}
\makeatletter
\@ifundefined{Shaded}{%
  \newenvironment{Shaded}{%
    \begin{tcolorbox}[enhanced,breakable,boxrule=0pt,sharp corners,%
      colback=shadecolor,left=6pt,right=6pt,top=6pt,bottom=6pt]%
  }{%
    \end{tcolorbox}%
  }%
}{%
  \renewenvironment{Shaded}{%
    \begin{tcolorbox}[enhanced,breakable,boxrule=0pt,sharp corners,%
      colback=shadecolor,left=6pt,right=6pt,top=6pt,bottom=6pt]%
  }{%
    \end{tcolorbox}%
  }%
}
\makeatother

% Cover theme colors
\definecolor{coverbgcolor}{HTML}{E0E0E0}
\definecolor{coverfgcolor}{HTML}{1F3134}
\definecolor{coverbar}{HTML}{7C9092}
\definecolor{bottomcolor}{HTML}{2C4F54}
\definecolor{nuanbai}{HTML}{F5F5F5}

% Beautybook options
\coverstyle={cover-choose=cn}
\mathstyle={math-font=plain}

% Disable default title page (Beautybook cover handles it)
\renewcommand{\maketitle}{}
